spring:
  config:
    activate:
      on-profile: dev
  application:
    name: thread-service
  datasource:
    url: jdbc:mysql://garamgaebee-rds.cmvufjnyvp1f.ap-northeast-2.rds.amazonaws.com:3306/threaddev?autoReconnect=true
    username: admin
    password: garamgaebeebackend!!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform:
    show-sql: true

  localhost: host.docker.internal
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://host.docker.internal:9000/eureka

# open api setting
openapi:
  service:
    title: thread-service doc #title that you will want to display in the swagger ui
    version: 1.0.0
    url: https://dev.garamgaebee.shop #api gateway url