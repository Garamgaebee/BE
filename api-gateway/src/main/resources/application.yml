server:
  servlet:
    context-path: /
  port: 8080

spring:
  main:
    web-application-type: reactive

  profiles:
    group:
      "local": "local,secret"
      "dev": "dev,secret"
    default: local
